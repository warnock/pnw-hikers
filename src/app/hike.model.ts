export class Hike {
  constructor(public name: string, public location: string, public type: string, public difficulty: number, public image: string, public shortDescription: string, public description: string, public notes: string, public recommendation: string) {}
}
